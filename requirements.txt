# GNN Weekend Project - Full Day Setup
# Python 3.12 | CUDA 12.x (RTX 3050)
# Install PyTorch with CUDA first (separately), then the rest:
#   pip install torch==2.5.1 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install torch-geometric
#   pip install -r requirements.txt

# ── Core GNN ──────────────────────────────────────────────
torch==2.5.1
torch-geometric==2.6.1
torch-scatter==2.1.2
torch-sparse==0.6.18
torch-cluster==1.6.3

# ── OGB (for stretch goal: ogbn-arxiv) ────────────────────
ogb==1.3.6

# ── Visualization ─────────────────────────────────────────
matplotlib==3.9.2
seaborn==0.13.2
networkx==3.3

# ── Dimensionality reduction (embedding plots) ─────────────
umap-learn==0.5.6
scikit-learn==1.5.2

# ── Utilities ─────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
tqdm==4.66.5
pyyaml==6.0.3